-- ==========================================================
-- V11__add_content_publish_caption.sql
--
-- 미리보기 화면에서 수정한 캡션의 보관처입니다.
--
-- publish_records.caption은 "요청 시점에 확정된 문구"라 발행 요청 전에는 존재하지 않습니다.
-- 수정본을 발행 전에도 남기려면 콘텐츠 쪽에 자리가 필요합니다.
--
-- NULL은 "수정한 적 없음"입니다. 그때는 PublishTextComposer가 매번 조립한 값을 쓰므로,
-- 콘텐츠를 고치면 캡션도 따라 갱신됩니다. 값이 들어간 뒤부터는 사용자가 쓴 문구가 이깁니다.
-- ==========================================================

ALTER TABLE contents
    ADD COLUMN publish_caption TEXT;
