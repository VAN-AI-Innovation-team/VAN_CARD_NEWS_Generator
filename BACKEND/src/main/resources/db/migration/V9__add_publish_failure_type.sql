-- ==========================================================
-- V9__add_publish_failure_type.sql
--
-- 발행 실패의 원인 분류를 기록합니다.
--
-- 재시도 가능 여부가 원인으로만 갈리므로(토큰 만료·이미지 fetch 실패는 재시도 무의미,
-- 레이트리밋·컨테이너 만료는 지연 후 재시도) 상태만으로는 다음 수를 정할 수 없습니다.
--
-- CHECK 제약을 두지 않은 것은 의도입니다. 유형이 늘 때마다 마이그레이션이 따라와야 하고,
-- 이 값은 애플리케이션 enum이 쓰는 내부 분류라 외부에서 임의 값이 들어올 경로가 없습니다.
-- ==========================================================

ALTER TABLE publish_records
    ADD COLUMN failure_type VARCHAR(30);
